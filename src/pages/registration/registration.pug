include ../../components/mixin.pug
doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(name="description", content="Страница регистрации на сайте TOXIN")
        meta(name="keywords", content="")
        title TOXIN
    body
        +header(false,"./login.html", "./registration.html")
        section(class="toxin-registration")
            +registration()
        +footer("subscription")