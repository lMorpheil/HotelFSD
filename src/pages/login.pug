include ../Blocks/mixin.pug
doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(name="description", content="Страница sign-in")
        meta(name="keywords", content="")
        title TOXIN
    body
        +header("",  "./login.html ", "./registration.html")
        section(class="toxin-login")
            +login(login)
        +footer("subscription")