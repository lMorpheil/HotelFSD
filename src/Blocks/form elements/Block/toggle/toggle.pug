mixin toggle(id,text,checked)
    .toggle
        if checked
            input(class="toggle__input" type="checkbox" id=`${id}` checked)
            label(class="toggle__label" for=`${id}`)=text
        else
            input(class="toggle__input" type="checkbox" id=`${id}`)
            label(class="toggle__label" for=`${id}`)=text