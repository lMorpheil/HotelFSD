mixin textfield(type, id, place, width, title, subtitle, mask, form)
    div.text-field
        if(title != undefined)
            label(class="text-field__label" style=`width: ${width}` for=`${id}`)
                span.text-field__title=title
                if(subtitle != undefined)
                    span.text-field__subtitle=subtitle
        input(
            id=`${id}`
            form=`${form}`
            type=(type == "subscription") ? "email" : 
                (type == "password") ? "password" : 
                (type == "date") ? "date" : "text"  
            class=(type == "subscription") ? "text-field__subscription" : 
            ( mask != undefined ) ? 'text-filed__input_date' : 'text-field__input'
            placeholder=`${place}` 
            style=`width: ${width}`
            checked
            )