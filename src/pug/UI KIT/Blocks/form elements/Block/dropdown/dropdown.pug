mixin dropdown(place, id, name, title, bed)
    div(class="dropdown")
        div(class="dropdown__wraplabel")
            label(for=`${id}` class="dropdown__label") 
                span=`${name}`
                span=`${title}`
        input(type="text" class="dropdown__input" readonly placeholder=`${place}` id=`${id}`)
        div(class="dropdown__window" data-window)
            div(class="dropdown__item" data-window)
                label(for="bedroom" class="dropdown__label" data-window) Спальни
                div
                    button(type="submit" class="dropdown__minus" data-window) -
                    input(type="text" class="dropdown__number" id="bedroom" value=`${bed}` data-window)
                    button(type="submit" class="dropdown__plus" data-window) +
            div(class="dropdown__item" data-window)
                label(for="bed" class="dropdown__label" data-window) Кровати
                div
                    button(type="submit" class="dropdown__minus" data-window) -
                    input(type="text" id="bed" class="dropdown__number" data-window)
                    button(type="submit" class="dropdown__plus" data-window) +
            div(class="dropdown__item" data-window)
                label(for="bath" class="dropdown__label" data-window) Ванные комнаты
                div
                    button(type="submit" class="dropdown__minus" data-window) -
                    input(type="text" id="bath" class="dropdown__number" data-window)
                    button(type="submit" class="dropdown__plus" data-window) +

